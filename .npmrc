# Do not fail installs on the Node engine range while the team migrates to Node 24+.
engine-strict=false
# Keep the dependency tree strict and predictable across all three developers.
shared-workspace-lockfile=true
