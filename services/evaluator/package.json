{
  "name": "@gradevision/evaluator",
  "version": "0.0.0",
  "private": true,
  "description": "GradeVision Insights evaluator service: consumes queued submissions, runs them in a sandbox (Judge0), grades against the rubric.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@gradevision/database": "workspace:*",
    "@gradevision/grading": "workspace:*",
    "@gradevision/queue": "workspace:*",
    "@gradevision/shared": "workspace:*",
    "dotenv": "^16.4.7",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  }
}
